// Input
// Output

// 1. Declaration
function test(a, b) {
  // function body
  return `${a} : ${b}`;
}

test('hello', 'world');

// 2. Expression

// 3. Arrow Function

let result = isNaN(90);

const fn = (el, index, array) => {};

fn()
  [(1, 4, 5)].map(el => {})
  [(1, 4, 5)].map(fn);
fn(el, index, array);
